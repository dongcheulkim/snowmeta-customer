# ===============================================
# 스노우메타 고객관리 시스템 환경 설정 파일
# ===============================================
# 실제 운영시 이 파일을 .env로 복사하여 사용하세요
# 보안상 실제 값들은 절대 깃허브에 올리지 마세요!

# 서버 기본 설정
NODE_ENV=production
PORT=5004

# 데이터베이스 설정 (실제 운영시 반드시 변경)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=snowmeta_customers
DB_USER=postgres
DB_PASSWORD=복잡한_패스워드_입력_필요
DB_POOL_MIN=2
DB_POOL_MAX=10

# 보안 설정 (JWT 토큰 및 세션)
JWT_SECRET=매우_복잡한_JWT_시크릿_키_입력_필요
SESSION_SECRET=매우_복잡한_세션_시크릿_키_입력_필요

# CORS 설정 (허용할 도메인들)
CORS_ORIGIN=https://snowmeta.co.kr,https://branch1.snowmeta.co.kr,https://branch2.snowmeta.co.kr

# SSL/HTTPS 설정 (인터넷 운영시 필수)
SSL_CERT_PATH=/path/to/ssl/certificate.crt
SSL_KEY_PATH=/path/to/ssl/private.key
ENABLE_HTTPS=true

# 개발/운영 환경 제어
GENERATE_SAMPLE_DATA=false
DEBUG_MODE=false

# 로깅 설정
LOG_LEVEL=info
LOG_FILE=./logs/snowmeta.log
ERROR_LOG_FILE=./logs/error.log

# 데이터베이스 백업 설정
DB_BACKUP_ENABLED=true
DB_BACKUP_SCHEDULE=0 2 * * *
DB_BACKUP_RETENTION_DAYS=30
DB_BACKUP_PATH=./backups

# 알림 설정 (이메일 등)
EMAIL_SERVICE=gmail
EMAIL_USER=admin@snowmeta.co.kr
EMAIL_PASSWORD=앱_비밀번호_입력
EMAIL_FROM=스노우메타 고객관리 시스템

# 모니터링 설정
ENABLE_MONITORING=true
HEALTH_CHECK_INTERVAL=30000

# 보안 헤더 설정
SECURITY_HEADERS_ENABLED=true
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100